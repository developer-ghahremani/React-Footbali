import MatchModel from "~/models/Match.model";
import React from "react";

type Props = {
  match: MatchModel;
};

const MatchDetail = (props: Props) => {
  return <div>MatchDetail</div>;
};

export default MatchDetail;
