import { MainLayout } from "~/components/layouts";
import React from "react";

type Props = {};

const Leagues = (props: Props) => {
  return <MainLayout>Leagues</MainLayout>;
};

export default Leagues;
